rules:
1. Only call hooks at the top level.don't call hooks inside loops, condition or nested function .
2. Only call hooks from react funcitons. call then only within react function not call in vanilla javascript.
